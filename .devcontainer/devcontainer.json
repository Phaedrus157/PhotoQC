{
  "name": "PhotoQC",
  "image": "mcr.microsoft.com/devcontainers/python:3.12",
  "features": {},
  "customizations": {
    "vscode": {
      "settings": {},
      "extensions": [
        "ms-python.python"
      ]
    }
  },
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y libgl1-mesa-glx && pip3 install --upgrade pip && pip3 install -r requirements.txt",
  "mounts": [
    "source=${localWorkspaceFolder},target=/workspaces/PhotoQC,type=bind"
  ],
  "remoteUser": "vscode"
}